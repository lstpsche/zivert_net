en:
  sidebar:
    tabs:
      headers:
        geo_point_details: GeoPoint Details
        map_settings: Map Settings
        user_measurements_history: My Measurement
      map_settings:
        labels:
          dimmer: Dim map
          geo_points: Geo Points
          regular_map: Regular map
        sections:
          base_map: Base map
          overlay_layers: Overlay layers
